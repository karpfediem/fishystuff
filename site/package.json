{
  "dependencies": {},
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.14",
    "@tailwindcss/typography": "^0.5.19",
    "daisyui": "^5.5.19",
    "iconify-icon": "^3.0.2",
    "tailwindcss": "^4.1.14"
  },
  "scripts": {
    "iconify:build": "bun build ./scripts/iconify-runtime.js --outfile assets/js/iconify.js --target browser --format iife --minify",
    "tailwind:scan": "bun run ./scripts/write-tailwind-scan.mjs",
    "tailwind:build": "bun run iconify:build && bun run tailwind:scan && bunx @tailwindcss/cli -i tailwind.input.css -o assets/css/site.css",
    "pre-build": "bun run tailwind:build",
    "build": "bun run iconify:build && bun run ./scripts/write-runtime-config.mjs --out public/runtime-config.js && bun run tailwind:scan && bunx @tailwindcss/cli -i tailwind.input.css -o public/css/site.css --minify"
  }
}
